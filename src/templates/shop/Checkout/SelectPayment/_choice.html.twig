{% import '@SyliusShop/shared/macro/money.html.twig' as money %}
{{ dump(hookable_metadata.context.form.parent[0]) }}
{# <div class="mb-3 d-flex justify-content-end">
    <span class="badge bg-secondary fs-5" {{ sylius_test_html_attribute('payment-method-fee') }}>
        {{ money.convertAndFormat(fee) }}
    </span>
</div> #}
